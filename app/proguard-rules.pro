# The app has no reflection-based serialization, so the defaults are enough.
# Keep line numbers in release stack traces.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
